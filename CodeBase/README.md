# WaveTone — CodeBase notes (deprecated)

This `CodeBase/README.md` contained duplicated top-level documentation and Docker instructions. The canonical project `README.md` at the repository root now includes the recommended Docker setup and best-practice guidance.

See the repository root `README.md` and root `docker-compose.yml` / `docker-compose.dev.yml` for production and development usage.

