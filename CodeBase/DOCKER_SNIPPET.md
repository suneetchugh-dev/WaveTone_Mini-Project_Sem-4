## Docker (deprecated in-folder snippet)

This snippet was previously stored in `CodeBase/DOCKER_SNIPPET.md`. The canonical Docker configuration and instructions have been moved to the repository root `README.md` and root `docker-compose.yml`.

Use the root `docker-compose.yml` for production and `docker-compose.dev.yml` for development (bind-mounts, nodemon, and vite dev server).

See `README.md` at the repository root for updated instructions and best practices.
